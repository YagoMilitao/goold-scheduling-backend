version: "3.9"

services:
  backend:
    build: .
    container_name: goold_backend
    env_file:
      - .env.tidb
    ports:
      - "3001:3001"
    volumes:
      - .:/app
      - ./certs:/app/certs:ro